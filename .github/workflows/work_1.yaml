name: work_1

on:
  push:
    branches:
      - main
    paths:
      - 'work_1/**'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Show hello message
        run: echo "Hello CI!"
